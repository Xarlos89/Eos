## The bot

The bot is also a very simple setup, The docker compose file waits for the aPI and the database to be healthy before bringing the bot online. In the dockerfile, we simply run the bot. 

The network connects it to the flask app. 